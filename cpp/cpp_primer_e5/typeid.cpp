#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[]) {
  cout << "typeid(int).name() : " << typeid(int).name() << endl;
  cout << "typeid(int).hash_code() : " << typeid(int).hash_code() << endl;

  return 0;
}
